on_run() {
    d: Dict = dict()
    dict_number_number_set(d, 1, 42)

    d2: Dict = dict()
    dict_number_id_set(d2, 2, d)

    d3: Dict = id_to_dict(dict_number_id_pop(d2, 2))
    n: number = dict_number_number_pop(d3, 1)
    assert_number(n, 42)
}
